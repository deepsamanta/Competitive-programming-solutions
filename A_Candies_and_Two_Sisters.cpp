#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    ll test;
    cin >> test;
    while (test--)
    {
        long double a;
        cin >> a;
        ll b = ceil(a / 2);
        cout << b - 1 << endl;
      
    }
}